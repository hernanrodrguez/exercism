#if !defined(FOOD_CHAIN_H)
#define FOOD_CHAIN_H

#include <iostream>
#include <string>
#include <vector>

namespace food_chain {

    using namespace std;

    string verse(int v);
    string verses(int init, int end);
    string sing();

}  // namespace food_chain

#endif // FOOD_CHAIN_H